# Production domain mapping
VITE_SITE_URL=https://news.mydomain.com

# REST API endpoint when database connection is migrated (empty by default for local simulation)
VITE_API_URL=https://api.mydomain.com/v1
